import React from "react";
import { createRootRoute } from "@tanstack/react-router";

// Use `any` to avoid strict type mismatch with generated route types in this workspace.
export const Route = createRootRoute({} as any);